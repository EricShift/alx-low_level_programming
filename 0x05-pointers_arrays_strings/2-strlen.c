#include "main.h"
#include <string.h>

/**
 * _strlen - Counts the number of chars in a string
 * @s: this is a string
 *
 * Return: An integer which is the number of chars
 */
int _strlen(char *s)
{

	int len;

	len = strlen(*s);
	return (len);
}
